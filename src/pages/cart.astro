---
import Base from "../layouts/Base.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const whatsappNumber = "5491158204344";
const mercadopagoPaymentLink =
  (import.meta.env.PUBLIC_MERCADOPAGO_PAYMENT_LINK || "https://link.mercadopago.com.ar/tiendafortunato").trim();
---

<Base
  title="Fortunato | Carrito"
  description="Revisá tu selección, pagá el subtotal por Mercado Pago y coordiná el envío por WhatsApp."
>
  <Header />

  <main class="cart-page">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Carrito</p>
        <h2>Tu selección Fortunato</h2>
        <p class="muted">Revisá cantidades, pagá el subtotal por Mercado Pago y seguí por WhatsApp para coordinar el envío.</p>
      </div>

      <div class="cart-list" data-cart-list></div>

      <div class="cart-summary" data-cart-summary hidden>
        <p><strong>Subtotal de productos</strong>: <span data-cart-subtotal>—</span></p>
        <div class="notice">
          <strong>Pagá primero el subtotal de productos:</strong> Ingresá exactamente ese monto en Mercado Pago.
          Después seguís por WhatsApp para validar el pago y coordinar envío.
        </div>
        <div class="cart-flow" aria-label="Cómo funciona el pago">
          <p><strong>1.</strong> Abrís el link oficial de Mercado Pago.</p>
          <p><strong>2.</strong> Pagás el subtotal exacto del carrito.</p>
          <p><strong>3.</strong> Confirmás por WhatsApp y coordinamos el envío.</p>
        </div>
        <p class="muted cart-summary-status" data-cart-status></p>
        <div class="cart-summary-actions">
          <button class="btn" type="button" data-cart-pay>Pagar subtotal con Mercado Pago</button>
          <button class="btn btn-ghost" type="button" data-cart-wa>Ya pagué, seguir por WhatsApp</button>
          <button class="btn btn-ghost" type="button" data-cart-clear>Vaciar selección</button>
        </div>
      </div>
    </div>
  </main>

  <Footer />

  <script define:vars={{ whatsappNumber, mercadopagoPaymentLink }}>
    const CART_KEY = "fortunato_cart";
    const list = document.querySelector("[data-cart-list]");
    const subtotalEl = document.querySelector("[data-cart-subtotal]");
    const summaryEl = document.querySelector("[data-cart-summary]");
    const clearBtn = document.querySelector("[data-cart-clear]");
    const payBtn = document.querySelector("[data-cart-pay]");
    const waBtn = document.querySelector("[data-cart-wa]");
    const statusEl = document.querySelector("[data-cart-status]");

    const getCart = () => {
      try { return JSON.parse(localStorage.getItem(CART_KEY) || "[]"); } catch { return []; }
    };
    const setCart = (cart) => localStorage.setItem(CART_KEY, JSON.stringify(cart));
    const safeImageSrc = (value) => {
      const src = String(value || "").trim();
      if (!src) return "/placeholder.svg";
      if (src.startsWith("/") || /^https?:\/\//i.test(src)) return src;
      return "/placeholder.svg";
    };

    const format = (amount, currency) =>
      typeof amount === "number" && !Number.isNaN(amount)
        ? `${currency} ${amount.toLocaleString("es-AR")}`
        : "Precio a confirmar";

    const makeKey = (item) => `${item.id}__${item.color || ""}__${item.size || ""}`;

    const setStatus = (text, type = "") => {
      if (!statusEl) return;
      statusEl.textContent = text || "";
      statusEl.dataset.type = type;
    };
    const createNode = (tag, className, text) => {
      const node = document.createElement(tag);
      if (className) node.className = className;
      if (typeof text === "string") node.textContent = text;
      return node;
    };
    const createCartItem = (item, qty, line) => {
      const article = createNode("article", "cart-item");
      article.dataset.key = makeKey(item);

      const image = createNode("img");
      image.src = safeImageSrc(item.image);
      image.alt = String(item.name || "Producto Fortunato");
      article.append(image);

      const main = createNode("div", "cart-item-main");
      const top = createNode("div", "cart-item-top");
      const name = createNode("strong", "cart-item-name", String(item.name || "Producto Fortunato"));
      const remove = createNode("button", "cart-remove", "×");
      remove.type = "button";
      remove.setAttribute("data-cart-remove", "");
      remove.setAttribute("aria-label", "Eliminar");
      top.append(name, remove);
      main.append(top);

      if (item.color) {
        main.append(createNode("p", "muted", `Color: ${String(item.color)}`));
      }
      if (item.size) {
        main.append(createNode("p", "muted", `Talle: ${String(item.size)}`));
      }

      const bottom = createNode("div", "cart-item-bottom");
      const qtyWrap = createNode("div", "cart-qty");
      qtyWrap.setAttribute("aria-label", "Cantidad");
      const dec = createNode("button", "cart-qty-btn", "−");
      dec.type = "button";
      dec.setAttribute("data-cart-dec", "");
      dec.setAttribute("aria-label", "Disminuir");
      const qtyValue = createNode("span", "cart-qty-value", String(qty));
      qtyValue.setAttribute("aria-live", "polite");
      const inc = createNode("button", "cart-qty-btn", "+");
      inc.type = "button";
      inc.setAttribute("data-cart-inc", "");
      inc.setAttribute("aria-label", "Aumentar");
      qtyWrap.append(dec, qtyValue, inc);

      const priceBlock = createNode("div", "cart-price-block");
      priceBlock.append(createNode("strong", "cart-line-total", format(line, item.currency || "ARS")));
      if (qty > 1) {
        priceBlock.append(
          createNode("span", "muted cart-each", `(c/u ${format(Number(item.price || 0), item.currency || "ARS")})`)
        );
      }

      bottom.append(qtyWrap, priceBlock);
      main.append(bottom);
      article.append(main);
      return article;
    };

    const updateBadge = () => {
      const badge = document.querySelector("[data-cart-count]");
      if (!badge) return;
      const count = getCart().reduce((sum, item) => sum + (item.qty || 0), 0);
      badge.textContent = count ? String(count) : "";
      badge.classList.toggle("is-visible", count > 0);
    };

    const render = () => {
      const cart = getCart();
      if (!cart.length) {
        list.replaceChildren(createNode("p", "muted", "Tu selección está vacía."));
        subtotalEl.textContent = "—";
        if (summaryEl) summaryEl.hidden = true;
        if (payBtn) payBtn.disabled = true;
        if (waBtn) waBtn.disabled = true;
        setStatus("");
        updateBadge();
        return;
      }
      if (summaryEl) summaryEl.hidden = false;
      if (payBtn) payBtn.disabled = false;
      if (waBtn) waBtn.disabled = false;
      let subtotal = 0;
      const rows = cart.map((item) => {
        const qty = Number(item.qty || 0);
        const price = Number(item.price || 0);
        const line = price * qty;
        subtotal += line;
        return createCartItem(item, qty, line);
      });
      list.replaceChildren(...rows);
      subtotalEl.textContent = format(subtotal, cart[0]?.currency || "ARS");
      updateBadge();
    };

    const buildWhatsappText = (cart, subtotalLabel) => {
      const lines = cart.map((item) => {
        const qty = Number(item.qty || 0);
        const price = Number(item.price || 0);
        const lineTotal = price * qty;
        const parts = [
          `- ${item.name} x${qty}`,
          item.color ? `Color: ${item.color}` : "",
          item.size ? `Talle: ${item.size}` : "",
          `— ${format(lineTotal, item.currency || "ARS")}`
        ].filter(Boolean);
        return parts.join(" · ");
      });

      return [
        "Hola! Quiero confirmar este pedido de Fortunato:",
        "",
        ...lines,
        "",
        `Subtotal pagado o a pagar por Mercado Pago: ${subtotalLabel}`,
        "",
        "Voy a usar o ya use el link de Mercado Pago de Fortunato. Necesito validar el pago y coordinar el envio."
      ].filter(Boolean).join("\n");
    };

    list?.addEventListener("click", (event) => {
      const row = event.target.closest("[data-key]");
      if (!row) return;
      const key = row.dataset.key;
      const cart = getCart();
      const index = cart.findIndex((item) => makeKey(item) === key);
      if (index === -1) return;

      if (event.target.closest("[data-cart-remove]")) {
        cart.splice(index, 1);
        setCart(cart);
        render();
        return;
      }

      if (event.target.closest("[data-cart-inc]")) {
        cart[index].qty = Number(cart[index].qty || 0) + 1;
        setCart(cart);
        render();
        return;
      }

      if (event.target.closest("[data-cart-dec]")) {
        const next = Number(cart[index].qty || 0) - 1;
        if (next <= 0) cart.splice(index, 1);
        else cart[index].qty = next;
        setCart(cart);
        render();
      }
    });

    clearBtn?.addEventListener("click", () => {
      const cart = getCart();
      if (!cart.length) return;
      if (!confirm("¿Vaciar selección?")) return;
      setCart([]);
      render();
    });

    payBtn?.addEventListener("click", () => {
      const cart = getCart();
      if (!cart.length) return;
      const next = window.open(mercadopagoPaymentLink, "_blank", "noopener,noreferrer");
      if (!next) window.location.href = mercadopagoPaymentLink;
      setStatus("Mercado Pago abierto. Pagá el subtotal exacto y después volvé para seguir por WhatsApp.", "pending");
    });

    waBtn?.addEventListener("click", () => {
      const cart = getCart();
      if (!cart.length) return;
      const subtotal = cart.reduce((sum, item) => sum + Number(item.price || 0) * Number(item.qty || 0), 0);
      const subtotalLabel = format(subtotal, cart[0]?.currency || "ARS");
      const text = buildWhatsappText(cart, subtotalLabel);
      setStatus("Abriendo WhatsApp...", "pending");
      window.location.href = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(text)}`;
    });

    render();
  </script>
</Base>
