---
import Base from "../layouts/Base.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const whatsappNumber = "5491158204344";
---

<Base title="Fortunato | Carrito" description="Revisá tu selección y cotizá el envío por WhatsApp.">
  <Header />

  <main class="cart-page">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Carrito</p>
        <h2>Tu selección Fortunato</h2>
        <p class="muted">Revisá cantidades y pedinos el total final con envío por WhatsApp.</p>
      </div>

      <div class="cart-list" data-cart-list></div>

      <div class="cart-summary" data-cart-summary hidden>
        <p><strong>Subtotal de productos</strong>: <span data-cart-subtotal>—</span></p>
        <div class="notice">
          <strong>Envío a confirmar:</strong> Te pasamos el costo de envío y el total final por WhatsApp. No transfieras hasta recibir esa confirmación.
        </div>
        <p class="muted cart-summary-status" data-cart-status></p>
        <div class="cart-summary-actions">
          <button class="btn" type="button" data-cart-wa>Cotizar envío por WhatsApp</button>
          <button class="btn btn-ghost" type="button" data-cart-clear>Vaciar selección</button>
        </div>
      </div>
    </div>
  </main>

  <Footer />

  <script define:vars={{ whatsappNumber }}>
    const CART_KEY = "fortunato_cart";
    const list = document.querySelector("[data-cart-list]");
    const subtotalEl = document.querySelector("[data-cart-subtotal]");
    const summaryEl = document.querySelector("[data-cart-summary]");
    const clearBtn = document.querySelector("[data-cart-clear]");
    const waBtn = document.querySelector("[data-cart-wa]");
    const statusEl = document.querySelector("[data-cart-status]");

    const getCart = () => {
      try { return JSON.parse(localStorage.getItem(CART_KEY) || "[]"); } catch { return []; }
    };
    const setCart = (cart) => localStorage.setItem(CART_KEY, JSON.stringify(cart));

    const format = (amount, currency) =>
      typeof amount === "number" && !Number.isNaN(amount)
        ? `${currency} ${amount.toLocaleString("es-AR")}`
        : "Precio a confirmar";

    const makeKey = (item) => `${item.id}__${item.color || ""}__${item.size || ""}`;

    const setStatus = (text, type = "") => {
      if (!statusEl) return;
      statusEl.textContent = text || "";
      statusEl.dataset.type = type;
    };

    const updateBadge = () => {
      const badge = document.querySelector("[data-cart-count]");
      if (!badge) return;
      const count = getCart().reduce((sum, item) => sum + (item.qty || 0), 0);
      badge.textContent = count ? String(count) : "";
      badge.classList.toggle("is-visible", count > 0);
    };

    const render = () => {
      const cart = getCart();
      if (!cart.length) {
        list.innerHTML = "<p class='muted'>Tu selección está vacía.</p>";
        subtotalEl.textContent = "—";
        if (summaryEl) summaryEl.hidden = true;
        if (waBtn) waBtn.disabled = true;
        setStatus("");
        updateBadge();
        return;
      }
      if (summaryEl) summaryEl.hidden = false;
      if (waBtn) waBtn.disabled = false;
      let subtotal = 0;
      list.innerHTML = cart.map((item) => {
        const qty = Number(item.qty || 0);
        const price = Number(item.price || 0);
        const line = price * qty;
        subtotal += line;
        const colorLine = item.color ? `<p class='muted'>Color: ${item.color}</p>` : "";
        const sizeLine = item.size ? `<p class='muted'>Talle: ${item.size}</p>` : "";
        const eachLine = qty > 1 ? `<span class='muted cart-each'>(c/u ${format(price, item.currency || "ARS")})</span>` : "";
        return `
          <article class='cart-item' data-key='${makeKey(item)}'>
            <img src='${item.image || "/placeholder.svg"}' alt='${item.name}' />
            <div class='cart-item-main'>
              <div class='cart-item-top'>
                <strong class='cart-item-name'>${item.name}</strong>
                <button class='cart-remove' type='button' data-cart-remove aria-label='Eliminar'>×</button>
              </div>
              ${colorLine}
              ${sizeLine}
              <div class='cart-item-bottom'>
                <div class='cart-qty' aria-label='Cantidad'>
                  <button type='button' class='cart-qty-btn' data-cart-dec aria-label='Disminuir'>−</button>
                  <span class='cart-qty-value' aria-live='polite'>${qty}</span>
                  <button type='button' class='cart-qty-btn' data-cart-inc aria-label='Aumentar'>+</button>
                </div>
                <div class='cart-price-block'>
                  <strong class='cart-line-total'>${format(line, item.currency || "ARS")}</strong>
                  ${eachLine}
                </div>
              </div>
            </div>
          </article>
        `;
      }).join("");
      subtotalEl.textContent = format(subtotal, cart[0]?.currency || "ARS");
      updateBadge();
    };

    const buildWhatsappText = (cart, subtotalLabel) => {
      const lines = cart.map((item) => {
        const qty = Number(item.qty || 0);
        const price = Number(item.price || 0);
        const lineTotal = price * qty;
        const parts = [
          `- ${item.name} x${qty}`,
          item.color ? `Color: ${item.color}` : "",
          item.size ? `Talle: ${item.size}` : "",
          `— ${format(lineTotal, item.currency || "ARS")}`
        ].filter(Boolean);
        return parts.join(" · ");
      });

      return [
        "Hola! Quiero cotizar envio y confirmar este pedido de Fortunato:",
        "",
        ...lines,
        "",
        `Subtotal de productos: ${subtotalLabel}`,
        "",
        "Pasenme por favor el costo de envio y el total final para transferir."
      ].filter(Boolean).join("\n");
    };

    list?.addEventListener("click", (event) => {
      const row = event.target.closest("[data-key]");
      if (!row) return;
      const key = row.dataset.key;
      const cart = getCart();
      const index = cart.findIndex((item) => makeKey(item) === key);
      if (index === -1) return;

      if (event.target.closest("[data-cart-remove]")) {
        cart.splice(index, 1);
        setCart(cart);
        render();
        return;
      }

      if (event.target.closest("[data-cart-inc]")) {
        cart[index].qty = Number(cart[index].qty || 0) + 1;
        setCart(cart);
        render();
        return;
      }

      if (event.target.closest("[data-cart-dec]")) {
        const next = Number(cart[index].qty || 0) - 1;
        if (next <= 0) cart.splice(index, 1);
        else cart[index].qty = next;
        setCart(cart);
        render();
      }
    });

    clearBtn?.addEventListener("click", () => {
      const cart = getCart();
      if (!cart.length) return;
      if (!confirm("¿Vaciar selección?")) return;
      setCart([]);
      render();
    });

    waBtn?.addEventListener("click", () => {
      const cart = getCart();
      if (!cart.length) return;
      const subtotal = cart.reduce((sum, item) => sum + Number(item.price || 0) * Number(item.qty || 0), 0);
      const subtotalLabel = format(subtotal, cart[0]?.currency || "ARS");
      const text = buildWhatsappText(cart, subtotalLabel);
      setStatus("Abriendo WhatsApp...", "pending");
      window.location.href = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(text)}`;
    });

    render();
  </script>
</Base>
